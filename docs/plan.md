# PawWeaver：当前目标与推进依据

**当前状态（E3）**：折足单项对照已完成且不采用：各100轮、983万交互，候选出现新的MuJoCo跌倒，任务与折足没有一致收益；见[负结果](../artifacts/runs/diagnostic_pose_learning/plan_v3_foot_fold/README.md)。现进入已授权的[速度＋末端目标方法比较](../artifacts/runs/diagnostic_pose_learning/plan_v3_commanded_pose/README.md)：独立279输入、单18关节Actor，任务系跟随base XY/yaw并固定地面Z，速度来自预设命令。CPU合同、初始双引擎站立与2轮训练入口已验证，fresh连续1000轮正在运行；约250/500轮MuJoCo开发读出已完成，后者全8例完整但尚无有效移动与整体操作成功；不把本路线称为世界固定EE-only成功。


**v3方案执行已恢复（2026-09-10）**：用户在新请求中明确要求执行 `/home/lyb/Desktop/PawWeaver_KISS_My_Agent_完整项目改进方案_2026-09-10_v3.md`。下文“本阶段最后一轮／整理后停止”保留为上一阶段历史边界，本次继续计划内开发与仿真；不是重新运行已完成的2000轮。先处理真实奖励信号、采样消费者、完整评估及完整更新保存，再按任务效果决定训练路线和预算。主代理唯一操作GPU，其他代理只做CPU或源码工作；本次不由文档内历史发布命令自动触发远程推送。世界系EE-only主路线、可选单Actor速度＋EE方法比较、下蹲／倾身／支撑移动目标、原验收和临时参数边界保留；正式朝向阈值仍未指定。

已完成的[E2固定任务课程](../artifacts/runs/diagnostic_pose_learning/plan_v3_task_curriculum/README.md)给出了连续1000轮及双引擎证据；其后的[折足成本对照](../artifacts/runs/diagnostic_pose_learning/plan_v3_foot_fold/README.md)未支持采用。当前下一步是完成顶部所述E3预算和双引擎终测，继续按实际任务与支撑判断。


以用户明确的全身控制能力目标和实际证据推进。《AS2 EDU + Piper-H 全身协同移动操作仿真方案》提供架构、场景和验证思路的参考；不要求逐条恢复全部模块、按固定阶段顺序执行或重做已经完成的工作。具体实验记录见 [runbook](runbook.md)，数值评价和证据边界见 [validation](validation.md)。

## 当前明确的能力目标

同一个强化学习 Actor 仅根据固定任务／世界系末端位置、朝向目标及本体观测，统一输出12腿＋6臂关节目标。机器人为了完成末端任务，自主协调机械臂、身体姿态与支撑：需要时下蹲、倾身、升降身体、转身和迈步；原地全身仍够不到时，稳定地移动过去完成任务。目标不随基座移动而重新锚定，不输入外部底盘速度命令或手写步态。

**2026-09-10新增路线授权**：用户要求核实EE-only与“底盘速度指令＋EE目标”两类方法，并明确两种都可以试。上述EE-only路线保留；比较分支允许给同一个18关节Actor增加外部底盘速度任务命令。新增分支必须明确EE目标的坐标系及部署时速度输入来源；若改变为随底盘的EE目标，不能把其成功报告为固定世界系EE-only任务通过，也不能把整套配方差异仅归因于增加速度输入。稳定支撑、完整臂姿态与全身协同效果、原性能数值和硬件证据边界保持。上一阶段仅完成了文献核实；本次v3执行已按条件进入速度指令比较分支，当前状态见顶部。 已核实：[UMI on Legs](https://umi-on-legs.github.io/static/umi-on-legs.pdf)摘要、§3.2与Fig.4使用不随基座移动的task-frame EE目标；Paw保存sim-world目标相当于选择固定任务基准，观测转换到当前base表达不改变目标固定性质，但不代表完整复制UMI坐标处理。[Deep Whole-Body Control](https://proceedings.mlr.press/v205/fu23a/fu23a.pdf)§2.1与Fig.4使用单18动作Actor、前向速度与yaw命令，EE参考随base水平位置／yaw移动且保持固定z；其渐进advantage mixing用于处理早期忽略locomotion的问题。这些是待考虑的方法条件，不证明输入差异已解释Paw失败，不构成输入差异的因果结论。

既要能到达给定目标，也要能持续跟踪移动目标。协调包含两个方向：机械臂伸展、旋转、俯仰造成负载变化时，腿和身体调整姿态与支撑保持稳定；四足运动或调整身体时，机械臂灵活补偿以保持世界系末端跟踪。训练轨迹应广泛覆盖组合机器人的操作空间，并包含需要移动支撑位置的目标。18维动作、训练有限、局部误差下降或基座移动均不单独证明上述能力；需要结合TCP误差、臂6关节实际运动、身体运动和足端接触判断。

已有方向保持：Isaac Lab训练，独立MuJoCo验证同一策略，状态控制稳定后接入图像测量驱动的目标。D435替代DC1已获授权；第一版仍为固定开度夹爪，保留负载，不加入抓取接触、力控制、复杂地形和真机部署。

## 用户指定的全身控制效果参考

2026-09-10，用户提供以下四个视频，并进一步明确：最终要达到的就是其中呈现的全身控制效果。交付须以连续运行展示身体、腿和机械臂对末端任务的合理、连贯、稳定配合，包括扩大操作范围、身体变化时的手臂补偿和需要时的支撑移动。当前策略的长期极端抬腿、贴限位支撑和远目标倾倒不算达成；误差与接触记录用于诊断和验证，不能替代实际动作效果，也不以原方案模块清单代替最终目标。

已通过公开视频的全时段稀疏抽帧及关键动作加密帧查看；下列时间是剪辑视频的播放时间，部分片段有加速或多画面剪辑。

| 视频与片段 | 对照的协同行为 |
|---|---|
| [Deep Whole-Body Control](https://www.bilibili.com/video/BV1mY411Z7ag/)，6–18秒、93–101秒 | 末端低／高／侧向运动伴随腿屈伸和身体倾斜；移动视觉目标带动臂与身体重新配合 |
| [MLM](https://www.bilibili.com/video/BV1AVbJz7EMN/)，81–95秒 | 支撑板变化时，腿、身体和机械臂共同调整，末端保持大致稳定的呈现方向；用于理解反向补偿 |
| [LeggedManip Lab](https://www.bilibili.com/video/BV1dAV66zEHR/)，12–26秒、28–40秒 | 仿真中的臂与身体姿态联动，以及真机迈步时保持可用的末端配置 |
| [Learning Dynamic Pick-and-Place](https://www.bilibili.com/video/BV1uY5s6ZECc/)，190–204秒 | 面向高低目标，屈腿降身、移动身体、伸臂和恢复站高共同完成操作范围转换 |

这些片段将用于对照实际策略的臂、身体和支撑行为，特别是身体帮助末端到达、末端运动时全身保持稳定，以及身体变化时手臂补偿三个方面。定量精度、稳定性和成功率仍以本项目完整测试记录判断；视频画面本身不能证明控制输入、因果机制或误差阈值。EE-only实现保持世界系末端目标；新增速度指令分支遵循上文授权和比较边界。参考效果不自动要求复制视频的算法、硬件、抓取任务或地形。

## 参考与可调整内容

- 原plan中关于世界系目标、全身任务驱动、分层排查Sim2Sim、接触与负载、独立测试的思路继续参考。
- 原plan的课程安排、MLM模块、预测器、速度估计器、示范数据和自适应采样是可选技术路径；按当前缺口和实测收益选择，不为完成清单而全部实现。
- 用户举例的空间盒、时间范围、人手速度及随机路点／minimum-jerk方法不是规定方案。采样、插值、奖励配方、并行规模和预算都是需要实际检验的工程选择。
- UMI实测数据不是当前依赖。已有可靠实现和证据继续复用；未标定的外部位姿不冒称本机器人原生TCP示范。
- 临时动力学参数允许已授权工程实验继续，但结果不能建立硬件有效性；不把参数未闭合用作停止所有学习工作的理由，也不把工程成功写成正式硬件验收。

## 本阶段最终状态

| 工作 | 已有证据与下一步 |
|---|---|
| 组合模型与控制入口 | URDF／USD／MJCF、276维位姿观测、单18输出Actor、显式PD及独立运行器已有实际运行证据；复用，不重建 |
| 广范围末端任务 | 新256条训练与64条独立采样测试轨迹已生成；包含不同高度、朝向和超原足支撑范围的目标。有限几何池和端点筛查不证明完整连续工作空间或动态可行性 |
| 实际学习 | 原2000轮完成；支撑配方336记录轮后中断、使用最新301轮检查点；低噪声最后一轮274记录轮后中断、使用最新251轮检查点，分别不含后35／23轮更新。两轮原定1000均未完成，均已完成六项后测和四段保存状态视频；GPU／仿真已释放 |
| 任务测试 | 低噪声251双引擎独立64例位置均0通过，跌倒16／27例；workspace4均完整但0通过，位置与朝向共同窗四例全退步；static16均0到达，跌倒12／9例。独立64位置RMSE .08671468→.15313607m／.08298025→.17285964m，朝向也退步。不采用新策略，原2000和支撑301失败边界保留，见[验证记录](validation.md) |
| 协同行为读出 | 支撑301的far有大腿净接触力；低噪声251的far位移增至.962／1.167m且保持段无非足净力>5N，但high仍高悬FL足.65–.70m、少足承重和支撑间歇突出。关节余量改善、身体升高或少跌倒均未同时保住跟踪与稳定支撑，不能称合理步态 |
| 最后一轮配置 | 从原2000起点，仅重置裁剪前腿／臂std为.25／.01rad，支撑config改entropy0、固定std；奖励、数据、PD、Actor、终止规则保留。4096×24、seed0、fresh Adam初始LR1e-5；持续退化促使提前停止，不称1000轮完成、探索的单因素因果或全部科研目标达标 |
| 本阶段交付与停止 | 已完成授权末轮的实际停止、原定六后测和四视频；现仅整理结果、本地提交，由root统一推送PawWeaver远程main后停止推进。不再加实验、预算、速度分支实现或视觉测试；速度＋EE路线仅完成文献核实，未完成的全身控制与视觉目标如实保留 |

## 沿用的评价基线与交付方向

保留既有评价数值用于连续比较，不因当前实验容易通过而放宽：静态目标水平距离约0.3–2m，10秒内到达5cm并保持1秒、成功率95%；连续目标速度不超过0.3m/s、完整60秒、过渡后位置RMSE不超过8cm／P95不超过15cm、至少95%回合无跌倒。原跨引擎参考规模为每引擎至少100回合、3个训练种子，到达成功率下降不超过10个百分点。小规模单种子诊断不冒充该规模；新增朝向正式阈值尚未确定。

完整时长和提前终止必须分别报告；前后误差按共同时间窗比较，不能让提前跌倒后的短均值制造改善。视觉测量误差、失跟和恢复另行评分，目标真值不进入视觉控制输入。新增方法只有实测带来任务收益才值得采用。

交付围绕可复现配置、资产、检查点、独立推理、测试结果与能展示实际全身行为的视频展开，具体实现随证据调整。当前全身移动操作能力尚未完成。

KMA同样以实际效果检验：是否帮助选对下一步、合理分工并暴露失败。只修观察到的问题；规则数量、安装、代理数量和软件检查不代表有效性。
